'use client';
import Broadcast from '@/src/components/Broadcast';
import React, { useRef } from 'react';

const page = () => {
	return (
		<div>
			<h1>Page1</h1>
			<Broadcast />
		</div>
	);
};

export default page;
